# optimizer info
optimizer:
  name: Adam
  kwargs:
    lr: 0.01
  other: 
    model: 0.0001
    emb_func: 0.001
    NEGMEAN: 0.0001


# lr_scheduler info
lr_scheduler:
  name: StepLR
  kwargs:
    gamma: 1.0
    step_size: 20

warmup: 0 # set 0 to turn off warmup
